name: Build static files

on:
  push:
    branches: [ 'main' ]

jobs:
  build-static-files:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: "Install npm"
        run: |
          sudo apt install npm
          sudo apt install reveal-md

      - name: Every time
        run: echo "This step will always run"